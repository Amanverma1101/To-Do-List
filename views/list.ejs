<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <link rel="stylesheet" href="https://fonts.googleapis.com/css2?family=Material+Symbols+Outlined:opsz,wght,FILL,GRAD@20..48,100..700,0..1,-50..200" />
    <link rel="stylesheet" href="/css/liststyle.css">
    <title>TO-Do_List</title>

</head>
<body>
    <main>
        <h1>To Do List</h1>
        <h2><%= kindOfDay %></h2>
        <section>
          <form action="/tasks" method="post">
            <input id="item" placeholder="Add Todo" name="newItem" autocomplete="off"/>
            <button class="addbtn">Add</button>
          </form>
        </section>
        <ul>
            <% let len=0; %> 
            <% if(newListItems !== undefined ) { len = newListItems.length} %> 
            <%  for(var i=0;i<len;i++){  %>
                <li for=`m${i}`>
                  <div class="litem">
                  <%= newListItems[i].task %> 
                  </div>

                 <div class="lright">
                  <form action="/del" method="post">
                    <label class="toggler-wrapper style-23">
                    <input type="checkbox" name="delbtn" value="<%= newListItems[i].id %>" onclick="this.form.submit()">
                    <div class="toggler-slider">
                      <div class="toggler-knob"></div>
                    </div>
                    </label> 
                      </form>
                      </div>
                </li>
            <% }  %>
        </ul>
        <button><a href="/">Logout</a></button>
        <aside id="completed-message">
          Todo marked complete!
        </aside>
      </main>
</body>
</html>